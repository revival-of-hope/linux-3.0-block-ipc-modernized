# Linux 3.0 块设备层与 IPC 子系统现代化修复报告

## 一、设计原则

本次修改遵循四项约束：

1. 不改变用户空间 ABI；
2. 不改变请求调度策略、IPC 权限模型或数据结构布局；
3. 优先处理整数溢出、错误输入和错误码丢失；
4. 所有新增核心算术必须能够脱离内核环境进行原生单元测试。

没有尝试把 Linux 3.0 的单队列块层整体替换为现代 `blk-mq`，也没有把旧 System V IPC 全面重写为当前内核实现。此类改造会跨越大量头文件、驱动和架构代码，不适合作为只有 `block/` 与 `ipc/` 两个目录的课程作业。

## 二、块设备层分析

### 1. 风险点：数组分配表达式

`blk-tag.c` 中的标签索引表和标签位图都属于“元素数 × 元素大小”的数组分配。旧写法让乘法在调用分配器之前发生。使用 `kcalloc()` 可把数组语义直接交给内核分配 API，并由其处理乘法溢出。

### 2. 风险点：宽松 sysfs 解析

队列参数直接影响请求数、预读大小、合并行为和 CPU affinity。配置接口不应接受部分有效、部分无效的字符串。改用 `kstrtoul()` 后，非法输入直接返回错误，调用者不再更新队列状态。

### 3. 性能影响

正常路径中：

- `kcalloc()` 与原 `kzalloc()` 都执行清零分配，时间复杂度和内存量不变；
- `DIV_ROUND_UP()` 只是更直接的整数表达式；
- sysfs 参数写入属于低频管理路径，严格校验的额外成本可以忽略。

因此优化重点不是提高 I/O 吞吐量，而是减少错误恢复成本、避免无效配置和提升维护性。对 Linux 3.0 做真正的高并发块层性能升级，需要引入后来的多队列架构，超出本作业合理范围。

## 三、IPC 子系统分析

### 1. 消息队列容量检查

旧实现的主要问题不是“完全没有检查”，而是检查顺序错误：表达式可能先在有符号类型中溢出，随后才转换。修复后每一步都通过无符号 `size_t` 边界判断，只有安全时才执行实际加法或乘法。

### 2. 信号量集合分配

信号量集合大小由固定头部与 `nsems` 个元素组成。改为两阶段检查后，错误原因明确为 `-EOVERFLOW`，不会把算术错误误报为普通内存不足；同时把 `used_sems + nsems` 改写为减法形式的配额判断，避免检查表达式本身先发生加法溢出。

### 3. IPC 分配接口

内存对象大小在 C 与内核分配 API 中应使用 `size_t`。原 `int` 参数会缩窄表达能力，并可能在较大值时截断。修改后调用点无需改变语义，编译器也能更好地检查类型。

### 4. 性能影响

每次对象创建只增加常数级的两次比较；对象创建相对于消息发送、信号量操作和共享内存访问并非高频核心循环。额外成本极低，避免异常大参数导致错误分配的收益更重要。

## 四、验证模型

### A. 可移植单元测试

直接编译生产代码使用的 `l30_checked_math.h`，覆盖：

- 普通加法和乘法；
- 零乘法；
- `SIZE_MAX + 1`；
- `SIZE_MAX × 2`；
- 消息队列单消息开销与总队列大小；
- 信号量数组头部与元素区域组合。

### B. 源码契约检查

Python 脚本检查修复后的生产源码，防止出现“测试代码正确，但生产源码没有使用”的情况。

### C. Kbuild 对象编译

远程工作流把补丁覆盖到官方 Linux 3.0 完整源码，然后编译五个真实对象。该步骤验证头文件、宏、函数声明、Kbuild 参数和链接前的翻译单元兼容性。

## 五、剩余限制

1. 只编译修改对象不能证明所有架构和所有 Kconfig 组合均通过；
2. 未进行真实块设备压力测试或 IPC 并发压力测试；
3. Linux 3.0 已非常陈旧，本补丁只适合教学与源码维护练习，不应被视为可部署的安全内核；
4. 上传包缺少完整源码，无法在原始附件内部直接生成内核镜像。

## 六、结论

本修改选择了两个子系统中最容易形成完整证据链的维护问题：数组分配与配置输入安全、IPC 大小计算与分配接口类型。修改量有限，但能同时体现现代内核编码原则、错误处理、整数安全、可测试性与构建验证，适合作为课程作业中的源码现代化案例。

## 9. CI 构建兼容性修订：宿主 PIE 与内核非 PIE 分离

首次 GitHub Actions 运行在 `scripts/basic/fixdep` 阶段失败。该程序是构建机上运行的宿主工具，不属于被修改的 Linux 内核对象。原工作流同时设置 `HOSTCFLAGS=-fno-pie` 与 `HOSTLDFLAGS=-no-pie`；但 Linux 3.0 的旧 `scripts/Makefile.host` 对单文件宿主程序不会可靠采用后者，造成非 PIE 目标代码被按 PIE 方式链接。

修订后不再覆盖宿主编译或链接标志，使 `fixdep` 等工具按 Ubuntu 默认方式构建为 PIE；仅通过 `KCFLAGS=-fcommon -fno-pie` 约束内核目标对象。这一分离符合宿主工具与内核代码的不同运行环境，也避免为解决构建机兼容性而污染内核源码。
