diff --git a/.github/workflows/linux30-object-build.yml b/.github/workflows/linux30-object-build.yml
index e6af22d90b0bd5d5bb67f7e54c6e3e69ed02661a..b8acb7248914b85181ca743438d7954c36d68756 100644
--- a/.github/workflows/linux30-object-build.yml
+++ b/.github/workflows/linux30-object-build.yml
@@ -48,6 +48,7 @@ jobs:
           cp block/scsi_ioctl.c linux-3.0/block/scsi_ioctl.c
           cp ipc/Makefile linux-3.0/ipc/Makefile
           cp ipc/alloc.c linux-3.0/ipc/alloc.c
+          cp ipc/alloc.h linux-3.0/ipc/alloc.h
           cp ipc/mqueue.c linux-3.0/ipc/mqueue.c
           cp ipc/msgutil.c linux-3.0/ipc/msgutil.c
           cp ipc/sem.c linux-3.0/ipc/sem.c
@@ -83,8 +84,10 @@ jobs:
             block/scsi_ioctl.o \
             ipc/alloc.o \
             ipc/mqueue.o \
+            ipc/msg.o \
             ipc/msgutil.o \
             ipc/sem.o \
+            ipc/shm.o \
             ipc/util.o; do
             test -s "linux-3.0/$object"
             cp "linux-3.0/$object" proof/
diff --git a/LARGE_REFACTOR.patch b/LARGE_REFACTOR.patch
index ece6a45169c3b55c5fe1d1d479fbc0f7ce74858c..69b60032b58eaf8e050c81cad52c60bac66cbae2 100644
--- a/LARGE_REFACTOR.patch
+++ b/LARGE_REFACTOR.patch
@@ -1,5 +1,5 @@
 diff --git a/.github/workflows/linux30-object-build.yml b/.github/workflows/linux30-object-build.yml
-index 3cff749..e6af22d 100644
+index 3cff7491d9896114dd39f58859c26caea7226924..b8acb7248914b85181ca743438d7954c36d68756 100644
 --- a/.github/workflows/linux30-object-build.yml
 +++ b/.github/workflows/linux30-object-build.yml
 @@ -1,4 +1,4 @@
@@ -26,7 +26,7 @@ index 3cff749..e6af22d 100644
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
-@@ -38,56 +38,64 @@ jobs:
+@@ -38,56 +38,67 @@ jobs:
              --output linux-3.0.tar.xz
            tar -xf linux-3.0.tar.xz
  
@@ -41,6 +41,7 @@ index 3cff749..e6af22d 100644
 +          cp block/scsi_ioctl.c linux-3.0/block/scsi_ioctl.c
 +          cp ipc/Makefile linux-3.0/ipc/Makefile
 +          cp ipc/alloc.c linux-3.0/ipc/alloc.c
++          cp ipc/alloc.h linux-3.0/ipc/alloc.h
            cp ipc/mqueue.c linux-3.0/ipc/mqueue.c
 +          cp ipc/msgutil.c linux-3.0/ipc/msgutil.c
            cp ipc/sem.c linux-3.0/ipc/sem.c
@@ -88,8 +89,10 @@ index 3cff749..e6af22d 100644
 +            block/scsi_ioctl.o \
 +            ipc/alloc.o \
 +            ipc/mqueue.o \
++            ipc/msg.o \
 +            ipc/msgutil.o \
 +            ipc/sem.o \
++            ipc/shm.o \
 +            ipc/util.o; do
 +            test -s "linux-3.0/$object"
 +            cp "linux-3.0/$object" proof/
@@ -111,7 +114,7 @@ index 3cff749..e6af22d 100644
            path: proof/
            if-no-files-found: error
 diff --git a/.gitignore b/.gitignore
-index fcb6a2f..86aeba8 100644
+index fcb6a2f4c6b9859395aa0cc73a3cd03b227f433d..86aeba8cc6275b42d0ab031c442ce1b57aee69b1 100644
 --- a/.gitignore
 +++ b/.gitignore
 @@ -1 +1,6 @@
@@ -123,10 +126,10 @@ index fcb6a2f..86aeba8 100644
 +linux-3.0.tar.xz
 +changed-objects-build.log
 diff --git a/MODERNIZATION_REPORT.md b/MODERNIZATION_REPORT.md
-index a81a272..087ebf8 100644
+index a81a272f6ea3b06247c7c6bd7238b8aad8b82f4c..45d8337863b9cf3c6a44168b947417f5613fd8dc 100644
 --- a/MODERNIZATION_REPORT.md
 +++ b/MODERNIZATION_REPORT.md
-@@ -1,88 +1,98 @@
+@@ -1,88 +1,103 @@
 -# Linux 3.0 块设备层与 IPC 子系统现代化修复报告
 +# Linux 3.0 `block/` / `ipc/` 项目审查与大规模重构报告
  
@@ -254,15 +257,20 @@ index a81a272..087ebf8 100644
 +### C. real Kbuild proof
  
 -本修改选择了两个子系统中最容易形成完整证据链的维护问题：数组分配与配置输入安全、IPC 大小计算与分配接口类型。修改量有限，但能同时体现现代内核编码原则、错误处理、整数安全、可测试性与构建验证，适合作为课程作业中的源码现代化案例。
-+GitHub Actions 将 9 个受影响对象放回官方 Linux 3.0 源码，在 Docker Official `gcc:4.8.5` 环境编译。该路径不再依赖 Ubuntu 14.04 的已退役 APT 仓库。它验证的是“真实 kernel translation unit 可以进入旧 Kbuild”，而不是用 mock header 假装编译内核。
++GitHub Actions 将 11 个受影响/接口消费者对象放回官方 Linux 3.0 源码，在 Docker Official `gcc:4.8.5` 环境编译。该路径不再依赖 Ubuntu 14.04 的已退役 APT 仓库。它验证的是“真实 kernel translation unit 可以进入旧 Kbuild”，而不是用 mock header 假装编译内核。
  
 -## 9. CI 构建兼容性修订：宿主 PIE 与内核非 PIE 分离
-+## 六、已执行结果
  
 -首次 GitHub Actions 运行在 `scripts/basic/fixdep` 阶段失败。该程序是构建机上运行的宿主工具，不属于被修改的 Linux 内核对象。原工作流同时设置 `HOSTCFLAGS=-fno-pie` 与 `HOSTLDFLAGS=-no-pie`；但 Linux 3.0 的旧 `scripts/Makefile.host` 对单文件宿主程序不会可靠采用后者，造成非 PIE 目标代码被按 PIE 方式链接。
-+当前沙箱实际执行：
++### D. 拆分模块的 include 自洽性
  
 -修订后不再覆盖宿主编译或链接标志，使 `fixdep` 等工具按 Ubuntu 默认方式构建为 PIE；仅通过 `KCFLAGS=-fcommon -fno-pie` 约束内核目标对象。这一分离符合宿主工具与内核代码的不同运行环境，也避免为解决构建机兼容性而污染内核源码。
++真实 GCC 4.8.5 Kbuild 首次编译 `ipc/alloc.o` 时暴露出一个结构性缺陷：新 translation unit 直接包含 `util.h`，但 `util.h` 使用 `IPCMNI`、`struct kern_ipc_perm` 等定义，原始 `util.c` 依靠此前包含的 `linux/shm.h`/`linux/msg.h` 间接提供这些定义。现已新增最小 `ipc/alloc.h`，使 allocator 模块不再依赖完整 `util.h`；同时 `util.h` 显式包含 `linux/ipc.h`，消除 include 顺序依赖。源码契约会拒绝 `alloc.c` 再次直接包含 `util.h`。
++
++## 六、已执行结果
++
++当前沙箱实际执行：
++
 +- GCC 14.2 portable build：2/2 tests PASS；
 +- Clang 17 portable build：2/2 tests PASS；
 +- source contracts：6/6 groups PASS；
@@ -277,10 +285,10 @@ index a81a272..087ebf8 100644
 +
 +相较原项目，本轮已经从“若干安全补丁”提升为“有明确模块边界、统一尺寸策略、广泛生产调用点覆盖和三层验证证据”的重构。它仍然有意避免全量 backport 现代内核架构，因为那会超出当前源码边界并显著降低可验证性。
 diff --git a/README_CN.md b/README_CN.md
-index bfae3c3..8b981f7 100644
+index bfae3c3982a4a6be0718e2f2853d4976491a4ae0..8a21deeda2ae44c3977baf6f81cd5db36027f6d9 100644
 --- a/README_CN.md
 +++ b/README_CN.md
-@@ -1,263 +1,226 @@
+@@ -1,263 +1,232 @@
 -# Linux 3.0 `block/` 与 `ipc/` 现代化修复包
 +# Linux 3.0 `block/` / `ipc/` 大规模可验证重构
  
@@ -499,12 +507,12 @@ index bfae3c3..8b981f7 100644
  
 -### 3.3 预期结果
 +它会从零建立临时 CMake build，并依次执行：
- 
--控制台末尾应出现：
++
 +1. 严格警告级别编译两个可移植 C 测试；
 +2. CTest 运行 checked-size 与 allocation-policy 测试；
 +3. Python 执行 6 组源码/架构契约。
-+
+ 
+-控制台末尾应出现：
 +当前重构环境中的实际结果：
  
  ```text
@@ -567,11 +575,9 @@ index bfae3c3..8b981f7 100644
 -.github/workflows/linux30-object-build.yml
 -```
 +因此，后续如果有人只删掉安全检查或把旧表达式改回来，本地验证会直接失败。
-+
-+## 5. 真实 Linux 3.0 Kbuild 验证
  
 -### 操作步骤
-+`.github/workflows/linux30-object-build.yml` 会：
++## 5. 真实 Linux 3.0 Kbuild 验证
  
 -1. 在 GitHub 新建一个空仓库；
 -2. 把本修复包全部文件提交并推送；
@@ -581,10 +587,12 @@ index bfae3c3..8b981f7 100644
 -6. 等待两个任务均显示绿色勾号；
 -7. 展开 `Build changed kernel objects`，确认以下对象均生成；
 -8. 在运行页面底部下载 `linux-3.0-modified-objects`，其中包含五个 `.o`、完整构建日志和 SHA-256 清单：
++`.github/workflows/linux30-object-build.yml` 会：
++
 +1. 下载官方 Linux 3.0 完整源码；
 +2. 覆盖本仓库中所有重构文件与 `ipc/Makefile`；
 +3. 在 Docker Official `gcc:4.8.5` 容器中运行 Linux 3.0 Kbuild；
-+4. 编译以下 9 个真实对象：
++4. 编译以下 11 个真实对象：
  
  ```text
  block/blk-tag.o
@@ -593,8 +601,10 @@ index bfae3c3..8b981f7 100644
 +block/scsi_ioctl.o
 +ipc/alloc.o
  ipc/mqueue.o
++ipc/msg.o
 +ipc/msgutil.o
  ipc/sem.o
++ipc/shm.o
  ipc/util.o
  ```
  
@@ -607,18 +617,22 @@ index bfae3c3..8b981f7 100644
 -- 再次运行 Windows 同源的可移植单元测试和源码约束检查。
 -
 -这比为了课程展示完整启动一个虚拟机更直接：验证目标是“补丁能否进入 Linux 3.0 的实际构建系统并生成对象文件”，而不是“内核能否在某个虚拟硬件上启动”。
--
++5. 再运行源码契约；
++6. 上传 `.o`、完整 build log 和 SHA-256 清单。
+ 
 -## 5. 将补丁应用到完整 Linux 3.0 源码
--
++Linux 3.0 的 `compiler-gcc${__GNUC__}.h` 机制只直接支持当时的 GCC 世代，因此这里故意使用 GCC 4.8，而不是把“把整个 Linux 3.0 迁移到 GCC 14”混入本次子系统重构。CI 直接使用 Docker Official `gcc:4.8.5` 历史镜像，并且 `ci/build-linux30-gcc48.sh` **不执行 `apt-get`**；这样旧编译器验证不再依赖 Ubuntu 14.04 已退役的软件仓库。源码契约会拒绝重新引入 `ubuntu:14.04`、`old-releases.ubuntu.com` 或运行时 APT 安装。
+ 
 -获得完整官方源码后，在源码根目录执行：
--
++### 5.4 IPC allocator 头文件隔离
+ 
 -```bash
 -patch -p1 < linux-3.0-block-ipc-modernization.patch
 -```
--
++真实 GCC 4.8 Kbuild 暴露出拆分后的 `ipc/alloc.c` 仍直接包含 `util.h`：原始 `util.c` 是在 `linux/shm.h`、`linux/msg.h` 等头文件之后才包含 `util.h`，因此可间接获得 `linux/ipc.h` 中的 `IPCMNI` 与 `struct kern_ipc_perm`；独立的 `alloc.c` 不具备这一 include 前置条件。修复后新增 `ipc/alloc.h`，仅声明 allocator/RCU lifetime 接口并直接包含 `linux/types.h`；`alloc.c` 只包含该最小接口，`util.h` 显式包含 `linux/ipc.h` 与 `alloc.h`。CI 同时增加 `ipc/msg.o`、`ipc/shm.o`，验证所有主要 SysV IPC allocator 调用方。
+ 
 -或者直接复制本包中的：
-+5. 再运行源码契约；
-+6. 上传 `.o`、完整 build log 和 SHA-256 清单。
++## 6. 验证结论边界
  
 -```text
 -block/blk-tag.c
@@ -629,11 +643,9 @@ index bfae3c3..8b981f7 100644
 -ipc/util.h
 -include/linux/l30_checked_math.h
 -```
-+Linux 3.0 的 `compiler-gcc${__GNUC__}.h` 机制只直接支持当时的 GCC 世代，因此这里故意使用 GCC 4.8，而不是把“把整个 Linux 3.0 迁移到 GCC 14”混入本次子系统重构。CI 直接使用 Docker Official `gcc:4.8.5` 历史镜像，并且 `ci/build-linux30-gcc48.sh` **不执行 `apt-get`**；这样旧编译器验证不再依赖 Ubuntu 14.04 已退役的软件仓库。源码契约会拒绝重新引入 `ubuntu:14.04`、`old-releases.ubuntu.com` 或运行时 APT 安装。
- 
+-
 -然后编译修改对象：
-+## 6. 验证结论边界
- 
+-
 -```bash
 -make defconfig
 -make prepare scripts
@@ -683,10 +695,10 @@ index bfae3c3..8b981f7 100644
 +SHA256SUMS.txt                       关键交付文件校验值
  ```
 diff --git a/SHA256SUMS.txt b/SHA256SUMS.txt
-index 0ca5a04..ec4bba0 100644
+index 0ca5a042f8aeefe820002f8354753aaaccce6e55..619f07aa0167170fccb36612ae88c214f11d4ed7 100644
 --- a/SHA256SUMS.txt
 +++ b/SHA256SUMS.txt
-@@ -1,58 +1,23 @@
+@@ -1,58 +1,25 @@
 -ef13ea11737070415c8f2f68df34fced15e3038251323ccf4a6b017c474fb84f  ./.github/workflows/linux30-object-build.yml
 -04dd9fb89be24f59f9b117cd93a0e3c1b356f593937f79e51c786e52042973ca  ./MODERNIZATION_REPORT.md
 -30eb95abe72e3be0d44aede36c6cafdd6ab6017ef8a5f59ae567769c648f1ee3  ./README_CN.md
@@ -745,34 +757,36 @@ index 0ca5a04..ec4bba0 100644
 -428049093a2cedef841e5aa4497f68916b5b672da2f7e5f57b658adc36a888f2  ./windows_verify/source_contract_test.py
 -3bffd097fb8f87e714a61be2a40382d80c9b82b30f09f56f766c5871c2b4388b  ./windows_verify/verify-native.cmd
 -88249c5b8dbc65b6071dc32e0503aedd8024746bdc15f7ed7a92ac61c2306b2f  ./windows_verify/verify-native.ps1
-+68fe415030cd7bd45cd29421017ffc9dae7c862235a1d8f18592c096e4a2efa9  README_CN.md
-+7caaf100f7a9c0fa7407eee003e0ba7bc959fd756dbe0e2ec4f9360a3e2e0714  MODERNIZATION_REPORT.md
-+f5c5712fea2f986b95c29ae511ed146d203edd33327c5de4c81c7295f3d2e1fa  VERIFICATION_STATUS.md
++314d1d976266eba23d8538f119e601474c0163285cc89aae04a9ba1aea2dd8ff  README_CN.md
++6b8a3fb3bd77419bc8a4e5f15d6163a98ee5bdf48c99624d9133f866c22efd7e  MODERNIZATION_REPORT.md
++67bc5d2a58e05c891e09ed89419cd9ca33027a24405b2a1fa4a6d60b0ec8b92d  VERIFICATION_STATUS.md
 +8f9f7c2ff2ea958eeca74ea222dad3ff15404fbf4cb873d94e18d93cff6da945  include/linux/l30_checked_math.h
 +3e1a92ea13d68933a6cf730a3092588561e15c2f956f8058d0d77a2908677057  block/blk-sysfs.c
 +af804248686f63815271f478f5d6b5ab1fce5fe19201e7063946e3cb9a032582  block/blk-tag.c
 +37e1f351e56ddfe3bcd0f021de0217b4844eb446bd09eba83ffcb72cb440601f  block/genhd.c
 +20baa44d6965b517338ef7623d43da1add2de7b390be522e4baf79492efda547  block/scsi_ioctl.c
 +aebc51e27c05ed2e2718f9c4fe6e7e1eb2f9d9d9999795139bdd68c76da3b7b7  ipc/Makefile
-+2b57e43b0286226b50a8718a12ef0d4caa41bc9d0497e8d456c200c6bfc8ee65  ipc/alloc.c
++64b6f9e5120bc387908144c942dad0608fdd75df3906196f9813adedf0ac4ccc  ipc/alloc.c
++cc5b1a1cd90d5adb386001ea9c45210bfa962a1b39ef1769b182bff8a75f73d5  ipc/alloc.h
 +1881bb91cfc1498cc031ab4ab494528ca959fc0821888a3baec91288fc89fea1  ipc/mqueue.c
 +f18c91bac7ea0050da8651d4b388e5b5ff52e5bc9fb3754eed5bf5f6bc0834d8  ipc/msgutil.c
 +e140d1fc2c74bdc459472ba4fd2291398ab4feab833dbc6164a2a42297fe856c  ipc/sem.c
 +2fe6086004f55c7a31401cb1efa46a68af3e2276930bc6c845ca41097ea030b3  ipc/util.c
-+2c506986faa148c0c50bf422b8f3ac575cb510417cff2e719bfabf5dfa44f8c0  ipc/util.h
++9bb97c2cb2204cb4f5e40e4316aeef3c9786c4887213b3d870f705e5fc80de90  ipc/util.h
 +9ef35ed058e3d66e08766f64b8ca4cec9aa1fcdcc602e24e4e2b7a15d02a64f9  windows_verify/CMakeLists.txt
 +0f5ee9bc1fac8055da7b66348c090cf2a1ba941995b42835dab1377186967fa1  windows_verify/checked_math_test.c
 +fa1b23f9d18ec1d0231fd17da9c6fff71c34a9a313ae47f909d8c1b12c29f4e8  windows_verify/allocation_policy_test.c
-+8ab5ffcc8dc190014ddb22cdf3608b5f121c8b3b419264f73ff4ca0458ff20a1  windows_verify/source_contract_test.py
++b6574f2c358ff323a6a6685bde35668ee52cd8ceca0a74b771e5f2d4225744e7  windows_verify/source_contract_test.py
 +165fb9e654d46177a1e468e9dbba7d4f5a330f288a7d77be8c4356e29ec207be  windows_verify/verify-local.sh
 +e4a8d28834db28c89c32182d32e6093cfe80d1deb0b2175f34158dbfca31564e  windows_verify/verify-native.ps1
-+b9a0d5b331406396420b815f4a341a83a5faef9e9255bd6add9a680c198cc536  .github/workflows/linux30-object-build.yml
-+09853d434bd15a695a1b6feb825739553441b0ea7e881cbeadc445f551c7809b  ci/build-linux30-gcc48.sh
++2b96cb3132d37d34c18c49d32ca62746f95310f432b4a947be49914eb69155c3  .github/workflows/linux30-object-build.yml
++dba2795bf3c5286914c75db7d88fd01d86200c0beb19c4d70bff3c240a081e26  ci/build-linux30-gcc48.sh
++1655ea151b766a0cf9e90d6ec6a4cf36967f1c0f533135be716405b39f60267a  .gitignore
 diff --git a/VERIFICATION_STATUS.md b/VERIFICATION_STATUS.md
-index 6780d93..c47c89c 100644
+index 6780d93097188ded001e6df536ef5ab03d04d3b4..1ffee1012cfa0de1621c097e1616cd670cb0bea5 100644
 --- a/VERIFICATION_STATUS.md
 +++ b/VERIFICATION_STATUS.md
-@@ -1,40 +1,71 @@
+@@ -1,40 +1,88 @@
 -# 验证状态记录
 +# 验证状态
  
@@ -812,7 +826,7 @@ index 6780d93..c47c89c 100644
 +4. 补丁回放
 +   - 从当前仓库改造前 `HEAD` 导出干净副本；
 +   - 应用 `LARGE_REFACTOR.patch`；
-+   - 22 个 patch 路径与最终工作树逐字节一致；
++   - 26 个 patch 路径与最终工作树逐字节一致；
 +   - 结果：`PATCH_APPLY_PASS`。
  
 -## 2026-08-05 GitHub Actions 首次运行故障与修复
@@ -832,31 +846,48 @@ index 6780d93..c47c89c 100644
 +## 已提供的最终编译验证
 +
 +GitHub Actions：
- 
- ```text
--relocation R_X86_64_32 against `.rodata' can not be used when making a PIE object
++
++```text
 +.github/workflows/linux30-object-build.yml
- ```
- 
--这不是 `block/` 或 `ipc/` 修改代码的编译错误。原因是旧版 Kbuild 对 `fixdep` 使用单条 `HOSTCC` 命令完成编译和链接；工作流曾向 `HOSTCFLAGS` 传入 `-fno-pie`，但 Linux 3.0 的该规则没有可靠采用单独给出的 `HOSTLDFLAGS=-no-pie`。结果是编译阶段生成非 PIE 重定位，链接阶段仍按 Ubuntu 默认设置生成 PIE，两者冲突。
++```
++
 +会在官方 Linux 3.0 源码上覆盖重构文件，并用 Docker Official `gcc:4.8.5` 编译：
 +
 +该 job 不再访问 Trusty APT 源，也不在历史工具链容器中动态安装软件包；因此不会再因 `old-releases.ubuntu.com` 的 404 而在真正编译前失败。
-+
-+```text
+ 
+ ```text
+-relocation R_X86_64_32 against `.rodata' can not be used when making a PIE object
 +block/blk-tag.o
 +block/blk-sysfs.o
 +block/genhd.o
 +block/scsi_ioctl.o
 +ipc/alloc.o
 +ipc/mqueue.o
++ipc/msg.o
 +ipc/msgutil.o
 +ipc/sem.o
++ipc/shm.o
 +ipc/util.o
-+```
+ ```
  
--修复方式：删除工作流中的 `HOSTCFLAGS` 和 `HOSTLDFLAGS` 覆盖，使宿主工具按发行版默认方式生成 PIE；`-fcommon -fno-pie` 只通过 `KCFLAGS` 施加到待验证的内核对象。该修复不改动任何内核生产源码。
+-这不是 `block/` 或 `ipc/` 修改代码的编译错误。原因是旧版 Kbuild 对 `fixdep` 使用单条 `HOSTCC` 命令完成编译和链接；工作流曾向 `HOSTCFLAGS` 传入 `-fno-pie`，但 Linux 3.0 的该规则没有可靠采用单独给出的 `HOSTLDFLAGS=-no-pie`。结果是编译阶段生成非 PIE 重定位，链接阶段仍按 Ubuntu 默认设置生成 PIE，两者冲突。
 +只有该 job 绿色通过，才能作为“这些真实 Linux 3.0 translation units 通过 Kbuild”的最终证据。
++
++### 第二轮真实 Kbuild 已暴露并修复的问题
++
++前一次 GitHub Actions 已经越过 Docker/Trusty 环境问题并进入 Linux 3.0 的真实 GCC 4.8.5 Kbuild。`block/blk-tag.o`、`block/blk-sysfs.o`、`block/genhd.o`、`block/scsi_ioctl.o` 均开始正常编译，失败发生在新拆出的 `ipc/alloc.o`：`alloc.c` 直接包含 `util.h`，而 `util.h` 使用 `IPCMNI` 与 `struct kern_ipc_perm`，这些定义在原始 `util.c` 中依赖此前的 `linux/shm.h`/`linux/msg.h` 间接引入。
++
++本版修复为：
++
++- 新增 `ipc/alloc.h`，只暴露 allocator/RCU lifetime 接口，并直接包含 `linux/types.h`；
++- `ipc/alloc.c` 只包含 `alloc.h`，不再拉入完整 `util.h`；
++- `ipc/util.h` 显式包含 `linux/ipc.h` 与 `alloc.h`，消除 include 顺序依赖；
++- CI overlay 新增 `ipc/alloc.h`；
++- 真实 Kbuild proof 增加 `ipc/msg.o` 与 `ipc/shm.o`，总计 11 个对象；
++- source contract 会拒绝 `alloc.c` 再次直接包含 `util.h`。
+ 
+-修复方式：删除工作流中的 `HOSTCFLAGS` 和 `HOSTLDFLAGS` 覆盖，使宿主工具按发行版默认方式生成 PIE；`-fcommon -fno-pie` 只通过 `KCFLAGS` 施加到待验证的内核对象。该修复不改动任何内核生产源码。
++因此，下一次 CI 若再失败，错误点应位于这个头文件依赖问题之后，而不会重复当前 `IPCMNI`/incomplete type 故障。
  
 -## 2026-08-07 CI toolchain correction (v3)
 +## 结论
@@ -870,7 +901,7 @@ index 6780d93..c47c89c 100644
 -Linux 3.0 to GCC 9 is part of this module modernization task.
 +当前已确认：重构后的核心尺寸策略可独立严格编译和执行，源码架构与禁止回归契约全部通过，工程脚本没有检测到格式/语法问题。真实 Linux 3.0 Kbuild 已完整自动化，但需要在 GitHub Actions 环境实际运行后获得最终对象级编译证据。
 diff --git a/block/blk-sysfs.c b/block/blk-sysfs.c
-index 7ba44e6..be58d8c 100644
+index 7ba44e64e772f20cd283a5782b564f5ea49fe5ef..be58d8cb900614bc59a62819ada1441268aabbcb 100644
 --- a/block/blk-sysfs.c
 +++ b/block/blk-sysfs.c
 @@ -22,18 +22,9 @@ queue_var_show(unsigned long var, char *page)
@@ -1023,7 +1054,7 @@ index 7ba44e6..be58d8c 100644
  	return ret;
  }
 diff --git a/block/genhd.c b/block/genhd.c
-index 3608289..9dfbc11 100644
+index 3608289c8ecdc0c8506af62421ccaabbae38438b..9dfbc1176cea0bcfd1ede67eac19e99369b7ebd4 100644
 --- a/block/genhd.c
 +++ b/block/genhd.c
 @@ -19,6 +19,7 @@
@@ -1062,7 +1093,7 @@ index 3608289..9dfbc11 100644
  	if (!new_ptbl)
  		return -ENOMEM;
 diff --git a/block/scsi_ioctl.c b/block/scsi_ioctl.c
-index 4f4230b..837ff24 100644
+index 4f4230b79bb6ee3c8164e462e40d4bdea3dc1fcd..837ff247f9a49a19ea784e79a484be5f488dc7b4 100644
 --- a/block/scsi_ioctl.c
 +++ b/block/scsi_ioctl.c
 @@ -26,6 +26,7 @@
@@ -1093,7 +1124,7 @@ index 4f4230b..837ff24 100644
  		if (!sg_iov) {
  			ret = -ENOMEM;
 diff --git a/ci/build-linux30-gcc48.sh b/ci/build-linux30-gcc48.sh
-index 69e4df1..cac5f0b 100644
+index 69e4df13b56be8746ee5aac192eaac77ac479d2d..8552f393902d9f59ea21e8132cfb45058606c51f 100644
 --- a/ci/build-linux30-gcc48.sh
 +++ b/ci/build-linux30-gcc48.sh
 @@ -1,24 +1,33 @@
@@ -1142,7 +1173,7 @@ index 69e4df1..cac5f0b 100644
  
  cd /work/linux-3.0
  make mrproper
-@@ -29,6 +38,10 @@ set -o pipefail
+@@ -29,6 +38,12 @@ set -o pipefail
  make -j2 V=1 \
    block/blk-tag.o \
    block/blk-sysfs.o \
@@ -1150,11 +1181,13 @@ index 69e4df1..cac5f0b 100644
 +  block/scsi_ioctl.o \
 +  ipc/alloc.o \
    ipc/mqueue.o \
++  ipc/msg.o \
 +  ipc/msgutil.o \
    ipc/sem.o \
++  ipc/shm.o \
    ipc/util.o 2>&1 | tee /work/changed-objects-build.log
 diff --git a/include/linux/l30_checked_math.h b/include/linux/l30_checked_math.h
-index b58365f..4d24f30 100644
+index b58365f2d5ebd76fdeb23155824a479fe26c4515..4d24f3044000cb720893f841897ba6db8215594a 100644
 --- a/include/linux/l30_checked_math.h
 +++ b/include/linux/l30_checked_math.h
 @@ -8,9 +8,16 @@
@@ -1214,7 +1247,7 @@ index b58365f..4d24f30 100644
 +
  #endif /* _LINUX_L30_CHECKED_MATH_H */
 diff --git a/ipc/Makefile b/ipc/Makefile
-index 9075e17..1d0b6cb 100644
+index 9075e172e52cacbcc4e5c0ba3d3ccb03d2a6d561..1d0b6cb258bd0d38659238c1e033e5d007d75779 100644
 --- a/ipc/Makefile
 +++ b/ipc/Makefile
 @@ -3,7 +3,7 @@
@@ -1226,8 +1259,217 @@ index 9075e17..1d0b6cb 100644
  obj-$(CONFIG_SYSVIPC_SYSCTL) += ipc_sysctl.o
  obj_mq-$(CONFIG_COMPAT) += compat_mq.o
  obj-$(CONFIG_POSIX_MQUEUE) += mqueue.o msgutil.o $(obj_mq-y)
+diff --git a/ipc/alloc.c b/ipc/alloc.c
+new file mode 100644
+index 0000000000000000000000000000000000000000..148d6357bb0fe7c806ef494787bfa05af89ce755
+--- /dev/null
++++ b/ipc/alloc.c
+@@ -0,0 +1,171 @@
++/*
++ * linux/ipc/alloc.c
++ *
++ * IPC allocation and RCU lifetime helpers split out of util.c.  Keeping
++ * allocation policy in one translation unit makes size validation auditable
++ * and prevents unrelated IPC identifier/permission code from accumulating
++ * memory-management details.
++ */
++
++#include <linux/kernel.h>
++#include <linux/mm.h>
++#include <linux/slab.h>
++#include <linux/vmalloc.h>
++#include <linux/rcupdate.h>
++#include <linux/workqueue.h>
++#include <linux/l30_checked_math.h>
++
++#include "alloc.h"
++
++/**
++ * ipc_alloc - allocate IPC scratch space
++ * @size: requested byte count
++ *
++ * Large allocations use vmalloc(), smaller ones use kmalloc().  The matching
++ * size must be supplied to ipc_free() so the same allocator family is used.
++ */
++void *ipc_alloc(size_t size)
++{
++	if (size > PAGE_SIZE)
++		return vmalloc(size);
++
++	return kmalloc(size, GFP_KERNEL);
++}
++
++/**
++ * ipc_free - free a block created by ipc_alloc()
++ * @ptr: allocation returned by ipc_alloc()
++ * @size: original requested byte count
++ */
++void ipc_free(void *ptr, size_t size)
++{
++	if (size > PAGE_SIZE)
++		vfree(ptr);
++	else
++		kfree(ptr);
++}
++
++/*
++ * RCU allocations prepend one of three headers to the user-visible object:
++ * ipc_rcu_hdr while live, ipc_rcu_grace during the grace period and, for
++ * vmalloc-backed objects, ipc_rcu_sched while vfree() is deferred to process
++ * context.  The lifetimes do not overlap, so the headers share storage.
++ */
++struct ipc_rcu_hdr {
++	int refcount;
++	int is_vmalloc;
++	void *data[0];
++};
++
++struct ipc_rcu_grace {
++	struct rcu_head rcu;
++	void *data[0];
++};
++
++struct ipc_rcu_sched {
++	struct work_struct work;
++	void *data[0];
++};
++
++#define HDRLEN_KMALLOC\
++	(sizeof(struct ipc_rcu_grace) > sizeof(struct ipc_rcu_hdr) ? \
++	 sizeof(struct ipc_rcu_grace) : sizeof(struct ipc_rcu_hdr))
++#define HDRLEN_VMALLOC\
++	(sizeof(struct ipc_rcu_sched) > HDRLEN_KMALLOC ? \
++	 sizeof(struct ipc_rcu_sched) : HDRLEN_KMALLOC)
++
++static inline int ipc_rcu_use_vmalloc(size_t size)
++{
++	/* HDRLEN_KMALLOC is far below PAGE_SIZE, but avoid an unchecked sum. */
++	return size > PAGE_SIZE - HDRLEN_KMALLOC;
++}
++
++static inline size_t ipc_rcu_header_size(int use_vmalloc)
++{
++	return use_vmalloc ? HDRLEN_VMALLOC : HDRLEN_KMALLOC;
++}
++
++static void ipc_rcu_init_header(void *object, int is_vmalloc)
++{
++	struct ipc_rcu_hdr *header;
++
++	header = container_of(object, struct ipc_rcu_hdr, data);
++	header->is_vmalloc = is_vmalloc;
++	header->refcount = 1;
++}
++
++/**
++ * ipc_rcu_alloc - allocate reference-counted IPC storage freed through RCU
++ * @size: user-visible object size
++ */
++void *ipc_rcu_alloc(size_t size)
++{
++	size_t allocation_size;
++	size_t header_size;
++	void *base;
++	void *object;
++	int use_vmalloc;
++
++	use_vmalloc = ipc_rcu_use_vmalloc(size);
++	header_size = ipc_rcu_header_size(use_vmalloc);
++	if (l30_size_add_overflow(header_size, size, &allocation_size))
++		return NULL;
++
++	if (use_vmalloc)
++		base = vmalloc(allocation_size);
++	else
++		base = kmalloc(allocation_size, GFP_KERNEL);
++	if (!base)
++		return NULL;
++
++	object = base + header_size;
++	ipc_rcu_init_header(object, use_vmalloc);
++	return object;
++}
++
++void ipc_rcu_getref(void *ptr)
++{
++	container_of(ptr, struct ipc_rcu_hdr, data)->refcount++;
++}
++
++static void ipc_do_vfree(struct work_struct *work)
++{
++	vfree(container_of(work, struct ipc_rcu_sched, work));
++}
++
++/* RCU callback context cannot call vfree(), so defer it to a work item. */
++static void ipc_schedule_free(struct rcu_head *head)
++{
++	struct ipc_rcu_grace *grace;
++	struct ipc_rcu_sched *sched;
++
++	grace = container_of(head, struct ipc_rcu_grace, rcu);
++	sched = container_of(&grace->data[0], struct ipc_rcu_sched, data[0]);
++
++	INIT_WORK(&sched->work, ipc_do_vfree);
++	schedule_work(&sched->work);
++}
++
++static void ipc_immediate_free(struct rcu_head *head)
++{
++	struct ipc_rcu_grace *free;
++
++	free = container_of(head, struct ipc_rcu_grace, rcu);
++	kfree(free);
++}
++
++void ipc_rcu_putref(void *ptr)
++{
++	struct ipc_rcu_hdr *header;
++	struct ipc_rcu_grace *grace;
++
++	header = container_of(ptr, struct ipc_rcu_hdr, data);
++	if (--header->refcount > 0)
++		return;
++
++	grace = container_of(ptr, struct ipc_rcu_grace, data);
++	if (header->is_vmalloc)
++		call_rcu(&grace->rcu, ipc_schedule_free);
++	else
++		call_rcu(&grace->rcu, ipc_immediate_free);
++}
+diff --git a/ipc/alloc.h b/ipc/alloc.h
+new file mode 100644
+index 0000000000000000000000000000000000000000..57af312be2e850e891695cc83cd6e9329446161d
+--- /dev/null
++++ b/ipc/alloc.h
+@@ -0,0 +1,26 @@
++/*
++ * linux/ipc/alloc.h
++ *
++ * Internal IPC allocation/lifetime interface.  Keep this header deliberately
++ * small so ipc/alloc.c does not need to pull in the full util.h dependency
++ * graph merely to verify its own public definitions.
++ */
++
++#ifndef _IPC_ALLOC_H
++#define _IPC_ALLOC_H
++
++#include <linux/types.h>
++
++/* Rare, potentially huge allocations.  Both functions may sleep. */
++void *ipc_alloc(size_t size);
++void ipc_free(void *ptr, size_t size);
++
++/*
++ * RCU-managed IPC storage. Objects start with reference count 1; the putref
++ * that reaches zero schedules destruction after the RCU grace period.
++ */
++void *ipc_rcu_alloc(size_t size);
++void ipc_rcu_getref(void *ptr);
++void ipc_rcu_putref(void *ptr);
++
++#endif /* _IPC_ALLOC_H */
 diff --git a/ipc/mqueue.c b/ipc/mqueue.c
-index b60844d..9f3f83a 100644
+index b60844d6e19a6364a31c8121e75b6c817d40b6cd..9f3f83a3a404eb11432df859f5dadcc8f12e85e5 100644
 --- a/ipc/mqueue.c
 +++ b/ipc/mqueue.c
 @@ -108,6 +108,19 @@ static struct ipc_namespace *get_ns_from_inode(struct inode *inode)
@@ -1342,7 +1584,7 @@ index b60844d..9f3f83a 100644
  
  	return 1;
 diff --git a/ipc/msgutil.c b/ipc/msgutil.c
-index 8b5ce5d..fbbf7d9 100644
+index 8b5ce5d3f3ef3e4f468d5afc4175cd22518fc029..fbbf7d94fa53666d9590a4a1821ad6419d46bcbc 100644
 --- a/ipc/msgutil.c
 +++ b/ipc/msgutil.c
 @@ -14,6 +14,7 @@
@@ -1390,7 +1632,7 @@ index 8b5ce5d..fbbf7d9 100644
  			err = -ENOMEM;
  			goto out_err;
 diff --git a/ipc/sem.c b/ipc/sem.c
-index 9ddd6f7..29e376d 100644
+index 9ddd6f786fa961f980a7da187c39921bea75baa5..29e376d52fe7611ba82f56e892fb532b29eeed3a 100644
 --- a/ipc/sem.c
 +++ b/ipc/sem.c
 @@ -242,7 +242,6 @@ static int newary(struct ipc_namespace *ns, struct ipc_params *params)
@@ -1525,7 +1767,7 @@ index 9ddd6f7..29e376d 100644
  		goto out_free;
  	}
 diff --git a/ipc/util.c b/ipc/util.c
-index 4146a4b..1d2ea7b 100644
+index 4146a4bb7dd9c952f4856d6e9869c8eff2efc092..1d2ea7bc875c606511fbffeb8a673e0dafba297e 100644
 --- a/ipc/util.c
 +++ b/ipc/util.c
 @@ -21,13 +21,11 @@
@@ -1728,12 +1970,49 @@ index 4146a4b..1d2ea7b 100644
  
  /**
   *	ipcperms	-	check IPC permissions
-diff --git a/linux-3.0-block-ipc-modernization.patch b/linux-3.0-block-ipc-modernization.patch
-deleted file mode 100644
-index 334d83f..0000000
---- a/linux-3.0-block-ipc-modernization.patch
-+++ /dev/null
-@@ -1,380 +0,0 @@
+diff --git a/ipc/util.h b/ipc/util.h
+index e92cf14794b25d9bb7ed38c4f94f7bca1a83631f..25ab073e630b4488bdd832ff77fdb681d82b0cf8 100644
+--- a/ipc/util.h
++++ b/ipc/util.h
+@@ -12,6 +12,9 @@
+ 
+ #include <linux/unistd.h>
+ #include <linux/err.h>
++#include <linux/ipc.h>
++
++#include "alloc.h"
+ 
+ #define SEQ_MULTIPLIER	(IPCMNI)
+ 
+@@ -105,22 +108,6 @@ void ipc_rmid(struct ipc_ids *, struct kern_ipc_perm *);
+ /* must be called with ipcp locked */
+ int ipcperms(struct ipc_namespace *ns, struct kern_ipc_perm *ipcp, short flg);
+ 
+-/* for rare, potentially huge allocations.
+- * both function can sleep
+- */
+-void *ipc_alloc(size_t size);
+-void ipc_free(void *ptr, size_t size);
+-
+-/*
+- * For allocation that need to be freed by RCU.
+- * Objects are reference counted, they start with reference count 1.
+- * getref increases the refcount, the putref call that reduces the recount
+- * to 0 schedules the rcu destruction. Caller must guarantee locking.
+- */
+-void *ipc_rcu_alloc(size_t size);
+-void ipc_rcu_getref(void *ptr);
+-void ipc_rcu_putref(void *ptr);
+-
+ struct kern_ipc_perm *ipc_lock(struct ipc_ids *, int);
+ 
+ void kernel_to_ipc64_perm(struct kern_ipc_perm *in, struct ipc64_perm *out);
+diff --git a/linux-3.0-block-ipc-modernization.patch b/linux-3.0-block-ipc-modernization.patch
+deleted file mode 100644
+index 334d83f8b52e46518bd050297b088abd6c84dd62..0000000000000000000000000000000000000000
+--- a/linux-3.0-block-ipc-modernization.patch
++++ /dev/null
+@@ -1,380 +0,0 @@
 ---- a/block/blk-tag.c
 -+++ b/block/blk-tag.c
 -@@ -111,18 +111,25 @@
@@ -2115,7 +2394,7 @@ index 334d83f..0000000
 -+
 -+#endif /* _LINUX_L30_CHECKED_MATH_H */
 diff --git a/windows_verify/CMakeLists.txt b/windows_verify/CMakeLists.txt
-index 0ea5d92..29dd8f8 100644
+index 0ea5d9245a64c38c377b75d06fd41d39683628e7..29dd8f87d8aa585bb16cf881912d0be3f66f4c8d 100644
 --- a/windows_verify/CMakeLists.txt
 +++ b/windows_verify/CMakeLists.txt
 @@ -7,13 +7,16 @@ set(CMAKE_C_EXTENSIONS OFF)
@@ -2144,8 +2423,91 @@ index 0ea5d92..29dd8f8 100644
 -add_test(NAME checked_math COMMAND checked_math_test)
 +add_policy_test(checked_math_test checked_math_test.c)
 +add_policy_test(allocation_policy_test allocation_policy_test.c)
+diff --git a/windows_verify/allocation_policy_test.c b/windows_verify/allocation_policy_test.c
+new file mode 100644
+index 0000000000000000000000000000000000000000..f4811884b6f75748cc190e9006e257e5477d9943
+--- /dev/null
++++ b/windows_verify/allocation_policy_test.c
+@@ -0,0 +1,77 @@
++#include <stdio.h>
++#include <stdlib.h>
++
++#include <linux/l30_checked_math.h>
++
++static void require_true(int condition, const char *message)
++{
++	if (!condition) {
++		fprintf(stderr, "FAIL: %s\n", message);
++		exit(EXIT_FAILURE);
++	}
++}
++
++static int mqueue_bytes(size_t max_messages, size_t message_size,
++			size_t *table_bytes, size_t *queue_bytes)
++{
++	size_t payload_bytes;
++
++	if (l30_size_array_bytes(max_messages, sizeof(void *), table_bytes) ||
++	    l30_size_array_bytes(max_messages, message_size, &payload_bytes) ||
++	    l30_size_add_overflow(*table_bytes, payload_bytes, queue_bytes))
++		return 1;
++	return 0;
++}
++
++static void test_mqueue_model(void)
++{
++	size_t table_bytes;
++	size_t queue_bytes;
++	const size_t max = (size_t)-1;
++
++	require_true(!mqueue_bytes(10, 8192, &table_bytes, &queue_bytes),
++		     "normal mqueue accounting must fit");
++	require_true(table_bytes == 10u * sizeof(void *),
++		     "mqueue pointer table must be exact");
++	require_true(queue_bytes == table_bytes + 81920u,
++		     "mqueue total must equal table plus payload");
++	require_true(mqueue_bytes(max, 8192, &table_bytes, &queue_bytes),
++		     "unbounded mqueue count must be rejected");
++}
++
++static void test_sem_models(void)
++{
++	size_t bytes;
++	const size_t max = (size_t)-1;
++
++	require_true(!l30_size_flex_bytes(256, 32000, 32, &bytes),
++		     "normal semaphore object must fit");
++	require_true(bytes == 1024256,
++		     "semaphore object model must be exact");
++	require_true(!l30_size_array_bytes(32000, sizeof(unsigned short), &bytes),
++		     "normal GETALL scratch buffer must fit");
++	require_true(bytes == 32000u * sizeof(unsigned short),
++		     "GETALL scratch buffer model must be exact");
++	require_true(l30_size_flex_bytes(256, max, sizeof(short), &bytes),
++		     "oversized sem_undo object must be rejected");
++}
++
++static void test_partition_and_iovec_models(void)
++{
++	size_t bytes;
++	const size_t max = (size_t)-1;
++
++	require_true(!l30_size_flex_bytes(64, 4096, sizeof(void *), &bytes),
++		     "normal partition table must fit");
++	require_true(l30_size_array_bytes(max, 16, &bytes),
++		     "oversized SCSI iovec table must be rejected");
++}
++
++int main(void)
++{
++	test_mqueue_model();
++	test_sem_models();
++	test_partition_and_iovec_models();
++	puts("PASS: refactored allocation policy models");
++	return EXIT_SUCCESS;
++}
 diff --git a/windows_verify/checked_math_test.c b/windows_verify/checked_math_test.c
-index 715b62c..f4b2935 100644
+index 715b62c6b84f02486d08b6522a216a92e29dde4a..f4b2935dcbf1de07a28d8da2dee8852141c42ca3 100644
 --- a/windows_verify/checked_math_test.c
 +++ b/windows_verify/checked_math_test.c
 @@ -43,48 +43,54 @@ static void test_multiply(void)
@@ -2233,7 +2595,7 @@ index 715b62c..f4b2935 100644
  	return EXIT_SUCCESS;
  }
 diff --git a/windows_verify/source_contract_test.py b/windows_verify/source_contract_test.py
-index 1221acd..d2c465e 100644
+index 1221acdb7636555719554e867481bbcf557c86b1..8e50cbed56c41d6ab7aac6cc40126e0e0f998374 100644
 --- a/windows_verify/source_contract_test.py
 +++ b/windows_verify/source_contract_test.py
 @@ -1,7 +1,14 @@
@@ -2252,7 +2614,7 @@ index 1221acd..d2c465e 100644
  import sys
  
  ROOT = Path(__file__).resolve().parents[1]
-@@ -18,55 +25,172 @@ def read(relative: str) -> str:
+@@ -18,55 +25,195 @@ def read(relative: str) -> str:
      return path.read_text(encoding="utf-8")
  
  
@@ -2282,6 +2644,7 @@ index 1221acd..d2c465e 100644
 +    makefile = read("ipc/Makefile")
 +    util = read("ipc/util.c")
 +    alloc = read("ipc/alloc.c")
++    alloc_h = read("ipc/alloc.h")
 +    util_h = read("ipc/util.h")
 +
 +    require("util.o alloc.o msgutil.o" in makefile,
@@ -2293,13 +2656,31 @@ index 1221acd..d2c465e 100644
 +    require("void *ipc_alloc(size_t size)" in alloc and
 +            "void *ipc_rcu_alloc(size_t size)" in alloc,
 +            "allocator implementations must live in ipc/alloc.c")
++    require('#include "alloc.h"' in alloc,
++            "ipc/alloc.c must include its focused interface header")
++    forbid(alloc, '#include "util.h"',
++           "ipc/alloc.c must not depend on the full IPC util.h graph")
++    require("#include <linux/types.h>" in alloc_h,
++            "ipc/alloc.h must define size_t through a direct Linux type dependency")
++    for prototype in (
++        "void *ipc_alloc(size_t size);",
++        "void ipc_free(void *ptr, size_t size);",
++        "void *ipc_rcu_alloc(size_t size);",
++        "void ipc_rcu_getref(void *ptr);",
++        "void ipc_rcu_putref(void *ptr);",
++    ):
++        require(prototype in alloc_h,
++                f"missing allocator interface prototype: {prototype}")
++    require('#include "alloc.h"' in util_h,
++            "ipc/util.h must re-export the focused allocator interface")
++    require("#include <linux/ipc.h>" in util_h,
++            "ipc/util.h must directly provide IPCMNI/kern_ipc_perm dependencies")
++    require("ipc_alloc(size_t size)" not in util_h,
++            "allocator prototypes must not be duplicated in ipc/util.h")
 +    require("ipc_rcu_init_header" in alloc and "ipc_rcu_header_size" in alloc,
 +            "RCU allocation policy must be factored into named helpers")
 +    require("l30_size_add_overflow(header_size, size, &allocation_size)" in alloc,
 +            "RCU header + object size must use checked addition")
-+    require("ipc_alloc(size_t size)" in util_h and
-+            "ipc_free(void *ptr, size_t size)" in util_h,
-+            "IPC allocation interface must use size_t")
 +
 +
 +def check_block_sysfs() -> None:
@@ -2427,14 +2808,18 @@ index 1221acd..d2c465e 100644
 +        "block/scsi_ioctl.o",
 +        "ipc/alloc.o",
 +        "ipc/mqueue.o",
++        "ipc/msg.o",
 +        "ipc/msgutil.o",
 +        "ipc/sem.o",
++        "ipc/shm.o",
 +        "ipc/util.o",
 +    )
 +    for obj in required_objects:
 +        require(obj in build, f"GCC 4.8 proof build does not compile {obj}")
 +    require("cp ipc/alloc.c linux-3.0/ipc/alloc.c" in workflow,
 +            "CI overlay must include new ipc/alloc.c")
++    require("cp ipc/alloc.h linux-3.0/ipc/alloc.h" in workflow,
++            "CI overlay must include focused ipc/alloc.h")
 +    require("cp ipc/Makefile linux-3.0/ipc/Makefile" in workflow,
 +            "CI overlay must include the refactored IPC Makefile")
 +    require("gcc:4.8.5" in workflow,
@@ -2465,8 +2850,37 @@ index 1221acd..d2c465e 100644
      return 0
  
  
+diff --git a/windows_verify/verify-local.sh b/windows_verify/verify-local.sh
+new file mode 100644
+index 0000000000000000000000000000000000000000..74345ddeaf74d3ff34e74a6c348187c9357c9b69
+--- /dev/null
++++ b/windows_verify/verify-local.sh
+@@ -0,0 +1,23 @@
++#!/usr/bin/env bash
++set -euo pipefail
++
++HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
++BUILD="${TMPDIR:-/tmp}/linux30-modernization-verify"
++
++command -v cmake >/dev/null
++command -v python3 >/dev/null
++
++rm -rf "$BUILD"
++echo '[1/4] Configure portable C verification'
++cmake -S "$HERE" -B "$BUILD"
++
++echo '[2/4] Build portable C verification with warnings-as-errors'
++cmake --build "$BUILD" -j2
++
++echo '[3/4] Run allocation policy tests'
++ctest --test-dir "$BUILD" --output-on-failure
++
++echo '[4/4] Run kernel-source architecture/regression contracts'
++python3 "$HERE/source_contract_test.py"
++
++echo 'PASS: local verification completed'
 diff --git a/windows_verify/verify-native.ps1 b/windows_verify/verify-native.ps1
-index 8738eff..54b9bef 100644
+index 8738effa5b401e7be25d019c6db394c6dfc11690..54b9bef635dfd0d99e88da785b5b8f5eed4d4300 100644
 --- a/windows_verify/verify-native.ps1
 +++ b/windows_verify/verify-native.ps1
 @@ -3,21 +3,26 @@ $ErrorActionPreference = "Stop"
@@ -2500,292 +2914,3 @@ index 8738eff..54b9bef 100644
  python (Join-Path $Here "source_contract_test.py")
  
  Write-Host "PASS: native Windows verification completed"
-diff --git a/ipc/alloc.c b/ipc/alloc.c
-new file mode 100644
-index 0000000..56a84fd
---- /dev/null
-+++ b/ipc/alloc.c
-@@ -0,0 +1,171 @@
-+/*
-+ * linux/ipc/alloc.c
-+ *
-+ * IPC allocation and RCU lifetime helpers split out of util.c.  Keeping
-+ * allocation policy in one translation unit makes size validation auditable
-+ * and prevents unrelated IPC identifier/permission code from accumulating
-+ * memory-management details.
-+ */
-+
-+#include <linux/kernel.h>
-+#include <linux/mm.h>
-+#include <linux/slab.h>
-+#include <linux/vmalloc.h>
-+#include <linux/rcupdate.h>
-+#include <linux/workqueue.h>
-+#include <linux/l30_checked_math.h>
-+
-+#include "util.h"
-+
-+/**
-+ * ipc_alloc - allocate IPC scratch space
-+ * @size: requested byte count
-+ *
-+ * Large allocations use vmalloc(), smaller ones use kmalloc().  The matching
-+ * size must be supplied to ipc_free() so the same allocator family is used.
-+ */
-+void *ipc_alloc(size_t size)
-+{
-+	if (size > PAGE_SIZE)
-+		return vmalloc(size);
-+
-+	return kmalloc(size, GFP_KERNEL);
-+}
-+
-+/**
-+ * ipc_free - free a block created by ipc_alloc()
-+ * @ptr: allocation returned by ipc_alloc()
-+ * @size: original requested byte count
-+ */
-+void ipc_free(void *ptr, size_t size)
-+{
-+	if (size > PAGE_SIZE)
-+		vfree(ptr);
-+	else
-+		kfree(ptr);
-+}
-+
-+/*
-+ * RCU allocations prepend one of three headers to the user-visible object:
-+ * ipc_rcu_hdr while live, ipc_rcu_grace during the grace period and, for
-+ * vmalloc-backed objects, ipc_rcu_sched while vfree() is deferred to process
-+ * context.  The lifetimes do not overlap, so the headers share storage.
-+ */
-+struct ipc_rcu_hdr {
-+	int refcount;
-+	int is_vmalloc;
-+	void *data[0];
-+};
-+
-+struct ipc_rcu_grace {
-+	struct rcu_head rcu;
-+	void *data[0];
-+};
-+
-+struct ipc_rcu_sched {
-+	struct work_struct work;
-+	void *data[0];
-+};
-+
-+#define HDRLEN_KMALLOC\
-+	(sizeof(struct ipc_rcu_grace) > sizeof(struct ipc_rcu_hdr) ? \
-+	 sizeof(struct ipc_rcu_grace) : sizeof(struct ipc_rcu_hdr))
-+#define HDRLEN_VMALLOC\
-+	(sizeof(struct ipc_rcu_sched) > HDRLEN_KMALLOC ? \
-+	 sizeof(struct ipc_rcu_sched) : HDRLEN_KMALLOC)
-+
-+static inline int ipc_rcu_use_vmalloc(size_t size)
-+{
-+	/* HDRLEN_KMALLOC is far below PAGE_SIZE, but avoid an unchecked sum. */
-+	return size > PAGE_SIZE - HDRLEN_KMALLOC;
-+}
-+
-+static inline size_t ipc_rcu_header_size(int use_vmalloc)
-+{
-+	return use_vmalloc ? HDRLEN_VMALLOC : HDRLEN_KMALLOC;
-+}
-+
-+static void ipc_rcu_init_header(void *object, int is_vmalloc)
-+{
-+	struct ipc_rcu_hdr *header;
-+
-+	header = container_of(object, struct ipc_rcu_hdr, data);
-+	header->is_vmalloc = is_vmalloc;
-+	header->refcount = 1;
-+}
-+
-+/**
-+ * ipc_rcu_alloc - allocate reference-counted IPC storage freed through RCU
-+ * @size: user-visible object size
-+ */
-+void *ipc_rcu_alloc(size_t size)
-+{
-+	size_t allocation_size;
-+	size_t header_size;
-+	void *base;
-+	void *object;
-+	int use_vmalloc;
-+
-+	use_vmalloc = ipc_rcu_use_vmalloc(size);
-+	header_size = ipc_rcu_header_size(use_vmalloc);
-+	if (l30_size_add_overflow(header_size, size, &allocation_size))
-+		return NULL;
-+
-+	if (use_vmalloc)
-+		base = vmalloc(allocation_size);
-+	else
-+		base = kmalloc(allocation_size, GFP_KERNEL);
-+	if (!base)
-+		return NULL;
-+
-+	object = base + header_size;
-+	ipc_rcu_init_header(object, use_vmalloc);
-+	return object;
-+}
-+
-+void ipc_rcu_getref(void *ptr)
-+{
-+	container_of(ptr, struct ipc_rcu_hdr, data)->refcount++;
-+}
-+
-+static void ipc_do_vfree(struct work_struct *work)
-+{
-+	vfree(container_of(work, struct ipc_rcu_sched, work));
-+}
-+
-+/* RCU callback context cannot call vfree(), so defer it to a work item. */
-+static void ipc_schedule_free(struct rcu_head *head)
-+{
-+	struct ipc_rcu_grace *grace;
-+	struct ipc_rcu_sched *sched;
-+
-+	grace = container_of(head, struct ipc_rcu_grace, rcu);
-+	sched = container_of(&grace->data[0], struct ipc_rcu_sched, data[0]);
-+
-+	INIT_WORK(&sched->work, ipc_do_vfree);
-+	schedule_work(&sched->work);
-+}
-+
-+static void ipc_immediate_free(struct rcu_head *head)
-+{
-+	struct ipc_rcu_grace *free;
-+
-+	free = container_of(head, struct ipc_rcu_grace, rcu);
-+	kfree(free);
-+}
-+
-+void ipc_rcu_putref(void *ptr)
-+{
-+	struct ipc_rcu_hdr *header;
-+	struct ipc_rcu_grace *grace;
-+
-+	header = container_of(ptr, struct ipc_rcu_hdr, data);
-+	if (--header->refcount > 0)
-+		return;
-+
-+	grace = container_of(ptr, struct ipc_rcu_grace, data);
-+	if (header->is_vmalloc)
-+		call_rcu(&grace->rcu, ipc_schedule_free);
-+	else
-+		call_rcu(&grace->rcu, ipc_immediate_free);
-+}
-diff --git a/windows_verify/allocation_policy_test.c b/windows_verify/allocation_policy_test.c
-new file mode 100644
-index 0000000..f481188
---- /dev/null
-+++ b/windows_verify/allocation_policy_test.c
-@@ -0,0 +1,77 @@
-+#include <stdio.h>
-+#include <stdlib.h>
-+
-+#include <linux/l30_checked_math.h>
-+
-+static void require_true(int condition, const char *message)
-+{
-+	if (!condition) {
-+		fprintf(stderr, "FAIL: %s\n", message);
-+		exit(EXIT_FAILURE);
-+	}
-+}
-+
-+static int mqueue_bytes(size_t max_messages, size_t message_size,
-+			size_t *table_bytes, size_t *queue_bytes)
-+{
-+	size_t payload_bytes;
-+
-+	if (l30_size_array_bytes(max_messages, sizeof(void *), table_bytes) ||
-+	    l30_size_array_bytes(max_messages, message_size, &payload_bytes) ||
-+	    l30_size_add_overflow(*table_bytes, payload_bytes, queue_bytes))
-+		return 1;
-+	return 0;
-+}
-+
-+static void test_mqueue_model(void)
-+{
-+	size_t table_bytes;
-+	size_t queue_bytes;
-+	const size_t max = (size_t)-1;
-+
-+	require_true(!mqueue_bytes(10, 8192, &table_bytes, &queue_bytes),
-+		     "normal mqueue accounting must fit");
-+	require_true(table_bytes == 10u * sizeof(void *),
-+		     "mqueue pointer table must be exact");
-+	require_true(queue_bytes == table_bytes + 81920u,
-+		     "mqueue total must equal table plus payload");
-+	require_true(mqueue_bytes(max, 8192, &table_bytes, &queue_bytes),
-+		     "unbounded mqueue count must be rejected");
-+}
-+
-+static void test_sem_models(void)
-+{
-+	size_t bytes;
-+	const size_t max = (size_t)-1;
-+
-+	require_true(!l30_size_flex_bytes(256, 32000, 32, &bytes),
-+		     "normal semaphore object must fit");
-+	require_true(bytes == 1024256,
-+		     "semaphore object model must be exact");
-+	require_true(!l30_size_array_bytes(32000, sizeof(unsigned short), &bytes),
-+		     "normal GETALL scratch buffer must fit");
-+	require_true(bytes == 32000u * sizeof(unsigned short),
-+		     "GETALL scratch buffer model must be exact");
-+	require_true(l30_size_flex_bytes(256, max, sizeof(short), &bytes),
-+		     "oversized sem_undo object must be rejected");
-+}
-+
-+static void test_partition_and_iovec_models(void)
-+{
-+	size_t bytes;
-+	const size_t max = (size_t)-1;
-+
-+	require_true(!l30_size_flex_bytes(64, 4096, sizeof(void *), &bytes),
-+		     "normal partition table must fit");
-+	require_true(l30_size_array_bytes(max, 16, &bytes),
-+		     "oversized SCSI iovec table must be rejected");
-+}
-+
-+int main(void)
-+{
-+	test_mqueue_model();
-+	test_sem_models();
-+	test_partition_and_iovec_models();
-+	puts("PASS: refactored allocation policy models");
-+	return EXIT_SUCCESS;
-+}
-diff --git a/windows_verify/verify-local.sh b/windows_verify/verify-local.sh
-new file mode 100755
-index 0000000..74345dd
---- /dev/null
-+++ b/windows_verify/verify-local.sh
-@@ -0,0 +1,23 @@
-+#!/usr/bin/env bash
-+set -euo pipefail
-+
-+HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
-+BUILD="${TMPDIR:-/tmp}/linux30-modernization-verify"
-+
-+command -v cmake >/dev/null
-+command -v python3 >/dev/null
-+
-+rm -rf "$BUILD"
-+echo '[1/4] Configure portable C verification'
-+cmake -S "$HERE" -B "$BUILD"
-+
-+echo '[2/4] Build portable C verification with warnings-as-errors'
-+cmake --build "$BUILD" -j2
-+
-+echo '[3/4] Run allocation policy tests'
-+ctest --test-dir "$BUILD" --output-on-failure
-+
-+echo '[4/4] Run kernel-source architecture/regression contracts'
-+python3 "$HERE/source_contract_test.py"
-+
-+echo 'PASS: local verification completed'
diff --git a/MODERNIZATION_REPORT.md b/MODERNIZATION_REPORT.md
index 087ebf81c48b1c91bf89995cb53cfc8e19034814..45d8337863b9cf3c6a44168b947417f5613fd8dc 100644
--- a/MODERNIZATION_REPORT.md
+++ b/MODERNIZATION_REPORT.md
@@ -77,7 +77,12 @@
 
 ### C. real Kbuild proof
 
-GitHub Actions 将 9 个受影响对象放回官方 Linux 3.0 源码，在 Docker Official `gcc:4.8.5` 环境编译。该路径不再依赖 Ubuntu 14.04 的已退役 APT 仓库。它验证的是“真实 kernel translation unit 可以进入旧 Kbuild”，而不是用 mock header 假装编译内核。
+GitHub Actions 将 11 个受影响/接口消费者对象放回官方 Linux 3.0 源码，在 Docker Official `gcc:4.8.5` 环境编译。该路径不再依赖 Ubuntu 14.04 的已退役 APT 仓库。它验证的是“真实 kernel translation unit 可以进入旧 Kbuild”，而不是用 mock header 假装编译内核。
+
+
+### D. 拆分模块的 include 自洽性
+
+真实 GCC 4.8.5 Kbuild 首次编译 `ipc/alloc.o` 时暴露出一个结构性缺陷：新 translation unit 直接包含 `util.h`，但 `util.h` 使用 `IPCMNI`、`struct kern_ipc_perm` 等定义，原始 `util.c` 依靠此前包含的 `linux/shm.h`/`linux/msg.h` 间接提供这些定义。现已新增最小 `ipc/alloc.h`，使 allocator 模块不再依赖完整 `util.h`；同时 `util.h` 显式包含 `linux/ipc.h`，消除 include 顺序依赖。源码契约会拒绝 `alloc.c` 再次直接包含 `util.h`。
 
 ## 六、已执行结果
 
diff --git a/README_CN.md b/README_CN.md
index 8b981f7ba0c7ec5c78a5909b2ef897e7579df7f5..8a21deeda2ae44c3977baf6f81cd5db36027f6d9 100644
--- a/README_CN.md
+++ b/README_CN.md
@@ -179,7 +179,7 @@ PowerShell 脚本会先删除旧 build，再重新配置、编译、测试和执
 1. 下载官方 Linux 3.0 完整源码；
 2. 覆盖本仓库中所有重构文件与 `ipc/Makefile`；
 3. 在 Docker Official `gcc:4.8.5` 容器中运行 Linux 3.0 Kbuild；
-4. 编译以下 9 个真实对象：
+4. 编译以下 11 个真实对象：
 
 ```text
 block/blk-tag.o
@@ -188,8 +188,10 @@ block/genhd.o
 block/scsi_ioctl.o
 ipc/alloc.o
 ipc/mqueue.o
+ipc/msg.o
 ipc/msgutil.o
 ipc/sem.o
+ipc/shm.o
 ipc/util.o
 ```
 
@@ -198,6 +200,10 @@ ipc/util.o
 
 Linux 3.0 的 `compiler-gcc${__GNUC__}.h` 机制只直接支持当时的 GCC 世代，因此这里故意使用 GCC 4.8，而不是把“把整个 Linux 3.0 迁移到 GCC 14”混入本次子系统重构。CI 直接使用 Docker Official `gcc:4.8.5` 历史镜像，并且 `ci/build-linux30-gcc48.sh` **不执行 `apt-get`**；这样旧编译器验证不再依赖 Ubuntu 14.04 已退役的软件仓库。源码契约会拒绝重新引入 `ubuntu:14.04`、`old-releases.ubuntu.com` 或运行时 APT 安装。
 
+### 5.4 IPC allocator 头文件隔离
+
+真实 GCC 4.8 Kbuild 暴露出拆分后的 `ipc/alloc.c` 仍直接包含 `util.h`：原始 `util.c` 是在 `linux/shm.h`、`linux/msg.h` 等头文件之后才包含 `util.h`，因此可间接获得 `linux/ipc.h` 中的 `IPCMNI` 与 `struct kern_ipc_perm`；独立的 `alloc.c` 不具备这一 include 前置条件。修复后新增 `ipc/alloc.h`，仅声明 allocator/RCU lifetime 接口并直接包含 `linux/types.h`；`alloc.c` 只包含该最小接口，`util.h` 显式包含 `linux/ipc.h` 与 `alloc.h`。CI 同时增加 `ipc/msg.o`、`ipc/shm.o`，验证所有主要 SysV IPC allocator 调用方。
+
 ## 6. 验证结论边界
 
 当前环境已经实际证明：
diff --git a/SHA256SUMS.txt b/SHA256SUMS.txt
index ec4bba09c5a52bda6162047f68b3acb3519d8086..619f07aa0167170fccb36612ae88c214f11d4ed7 100644
--- a/SHA256SUMS.txt
+++ b/SHA256SUMS.txt
@@ -1,23 +1,25 @@
-68fe415030cd7bd45cd29421017ffc9dae7c862235a1d8f18592c096e4a2efa9  README_CN.md
-7caaf100f7a9c0fa7407eee003e0ba7bc959fd756dbe0e2ec4f9360a3e2e0714  MODERNIZATION_REPORT.md
-f5c5712fea2f986b95c29ae511ed146d203edd33327c5de4c81c7295f3d2e1fa  VERIFICATION_STATUS.md
+314d1d976266eba23d8538f119e601474c0163285cc89aae04a9ba1aea2dd8ff  README_CN.md
+6b8a3fb3bd77419bc8a4e5f15d6163a98ee5bdf48c99624d9133f866c22efd7e  MODERNIZATION_REPORT.md
+67bc5d2a58e05c891e09ed89419cd9ca33027a24405b2a1fa4a6d60b0ec8b92d  VERIFICATION_STATUS.md
 8f9f7c2ff2ea958eeca74ea222dad3ff15404fbf4cb873d94e18d93cff6da945  include/linux/l30_checked_math.h
 3e1a92ea13d68933a6cf730a3092588561e15c2f956f8058d0d77a2908677057  block/blk-sysfs.c
 af804248686f63815271f478f5d6b5ab1fce5fe19201e7063946e3cb9a032582  block/blk-tag.c
 37e1f351e56ddfe3bcd0f021de0217b4844eb446bd09eba83ffcb72cb440601f  block/genhd.c
 20baa44d6965b517338ef7623d43da1add2de7b390be522e4baf79492efda547  block/scsi_ioctl.c
 aebc51e27c05ed2e2718f9c4fe6e7e1eb2f9d9d9999795139bdd68c76da3b7b7  ipc/Makefile
-2b57e43b0286226b50a8718a12ef0d4caa41bc9d0497e8d456c200c6bfc8ee65  ipc/alloc.c
+64b6f9e5120bc387908144c942dad0608fdd75df3906196f9813adedf0ac4ccc  ipc/alloc.c
+cc5b1a1cd90d5adb386001ea9c45210bfa962a1b39ef1769b182bff8a75f73d5  ipc/alloc.h
 1881bb91cfc1498cc031ab4ab494528ca959fc0821888a3baec91288fc89fea1  ipc/mqueue.c
 f18c91bac7ea0050da8651d4b388e5b5ff52e5bc9fb3754eed5bf5f6bc0834d8  ipc/msgutil.c
 e140d1fc2c74bdc459472ba4fd2291398ab4feab833dbc6164a2a42297fe856c  ipc/sem.c
 2fe6086004f55c7a31401cb1efa46a68af3e2276930bc6c845ca41097ea030b3  ipc/util.c
-2c506986faa148c0c50bf422b8f3ac575cb510417cff2e719bfabf5dfa44f8c0  ipc/util.h
+9bb97c2cb2204cb4f5e40e4316aeef3c9786c4887213b3d870f705e5fc80de90  ipc/util.h
 9ef35ed058e3d66e08766f64b8ca4cec9aa1fcdcc602e24e4e2b7a15d02a64f9  windows_verify/CMakeLists.txt
 0f5ee9bc1fac8055da7b66348c090cf2a1ba941995b42835dab1377186967fa1  windows_verify/checked_math_test.c
 fa1b23f9d18ec1d0231fd17da9c6fff71c34a9a313ae47f909d8c1b12c29f4e8  windows_verify/allocation_policy_test.c
-8ab5ffcc8dc190014ddb22cdf3608b5f121c8b3b419264f73ff4ca0458ff20a1  windows_verify/source_contract_test.py
+b6574f2c358ff323a6a6685bde35668ee52cd8ceca0a74b771e5f2d4225744e7  windows_verify/source_contract_test.py
 165fb9e654d46177a1e468e9dbba7d4f5a330f288a7d77be8c4356e29ec207be  windows_verify/verify-local.sh
 e4a8d28834db28c89c32182d32e6093cfe80d1deb0b2175f34158dbfca31564e  windows_verify/verify-native.ps1
-b9a0d5b331406396420b815f4a341a83a5faef9e9255bd6add9a680c198cc536  .github/workflows/linux30-object-build.yml
-09853d434bd15a695a1b6feb825739553441b0ea7e881cbeadc445f551c7809b  ci/build-linux30-gcc48.sh
+2b96cb3132d37d34c18c49d32ca62746f95310f432b4a947be49914eb69155c3  .github/workflows/linux30-object-build.yml
+dba2795bf3c5286914c75db7d88fd01d86200c0beb19c4d70bff3c240a081e26  ci/build-linux30-gcc48.sh
+1655ea151b766a0cf9e90d6ec6a4cf36967f1c0f533135be716405b39f60267a  .gitignore
diff --git a/VERIFICATION_STATUS.md b/VERIFICATION_STATUS.md
index c47c89c13951a2709751de8e3d0780f625afaf91..1ffee1012cfa0de1621c097e1616cd670cb0bea5 100644
--- a/VERIFICATION_STATUS.md
+++ b/VERIFICATION_STATUS.md
@@ -25,7 +25,7 @@
 4. 补丁回放
    - 从当前仓库改造前 `HEAD` 导出干净副本；
    - 应用 `LARGE_REFACTOR.patch`；
-   - 22 个 patch 路径与最终工作树逐字节一致；
+   - 26 个 patch 路径与最终工作树逐字节一致；
    - 结果：`PATCH_APPLY_PASS`。
 
 ## 当前环境不能真实执行的验证
@@ -59,13 +59,30 @@ block/genhd.o
 block/scsi_ioctl.o
 ipc/alloc.o
 ipc/mqueue.o
+ipc/msg.o
 ipc/msgutil.o
 ipc/sem.o
+ipc/shm.o
 ipc/util.o
 ```
 
 只有该 job 绿色通过，才能作为“这些真实 Linux 3.0 translation units 通过 Kbuild”的最终证据。
 
+### 第二轮真实 Kbuild 已暴露并修复的问题
+
+前一次 GitHub Actions 已经越过 Docker/Trusty 环境问题并进入 Linux 3.0 的真实 GCC 4.8.5 Kbuild。`block/blk-tag.o`、`block/blk-sysfs.o`、`block/genhd.o`、`block/scsi_ioctl.o` 均开始正常编译，失败发生在新拆出的 `ipc/alloc.o`：`alloc.c` 直接包含 `util.h`，而 `util.h` 使用 `IPCMNI` 与 `struct kern_ipc_perm`，这些定义在原始 `util.c` 中依赖此前的 `linux/shm.h`/`linux/msg.h` 间接引入。
+
+本版修复为：
+
+- 新增 `ipc/alloc.h`，只暴露 allocator/RCU lifetime 接口，并直接包含 `linux/types.h`；
+- `ipc/alloc.c` 只包含 `alloc.h`，不再拉入完整 `util.h`；
+- `ipc/util.h` 显式包含 `linux/ipc.h` 与 `alloc.h`，消除 include 顺序依赖；
+- CI overlay 新增 `ipc/alloc.h`；
+- 真实 Kbuild proof 增加 `ipc/msg.o` 与 `ipc/shm.o`，总计 11 个对象；
+- source contract 会拒绝 `alloc.c` 再次直接包含 `util.h`。
+
+因此，下一次 CI 若再失败，错误点应位于这个头文件依赖问题之后，而不会重复当前 `IPCMNI`/incomplete type 故障。
+
 ## 结论
 
 当前已确认：重构后的核心尺寸策略可独立严格编译和执行，源码架构与禁止回归契约全部通过，工程脚本没有检测到格式/语法问题。真实 Linux 3.0 Kbuild 已完整自动化，但需要在 GitHub Actions 环境实际运行后获得最终对象级编译证据。
diff --git a/ci/build-linux30-gcc48.sh b/ci/build-linux30-gcc48.sh
index cac5f0b7fc036df9e50d9a13f8f60b3b267dd91e..8552f393902d9f59ea21e8132cfb45058606c51f 100755
--- a/ci/build-linux30-gcc48.sh
+++ b/ci/build-linux30-gcc48.sh
@@ -42,6 +42,8 @@ make -j2 V=1 \
   block/scsi_ioctl.o \
   ipc/alloc.o \
   ipc/mqueue.o \
+  ipc/msg.o \
   ipc/msgutil.o \
   ipc/sem.o \
+  ipc/shm.o \
   ipc/util.o 2>&1 | tee /work/changed-objects-build.log
diff --git a/ipc/alloc.c b/ipc/alloc.c
index 56a84fd273c9306f47e55c3732bd53cfcd4b1a7d..148d6357bb0fe7c806ef494787bfa05af89ce755 100644
--- a/ipc/alloc.c
+++ b/ipc/alloc.c
@@ -15,7 +15,7 @@
 #include <linux/workqueue.h>
 #include <linux/l30_checked_math.h>
 
-#include "util.h"
+#include "alloc.h"
 
 /**
  * ipc_alloc - allocate IPC scratch space
diff --git a/ipc/alloc.h b/ipc/alloc.h
new file mode 100644
index 0000000000000000000000000000000000000000..57af312be2e850e891695cc83cd6e9329446161d
--- /dev/null
+++ b/ipc/alloc.h
@@ -0,0 +1,26 @@
+/*
+ * linux/ipc/alloc.h
+ *
+ * Internal IPC allocation/lifetime interface.  Keep this header deliberately
+ * small so ipc/alloc.c does not need to pull in the full util.h dependency
+ * graph merely to verify its own public definitions.
+ */
+
+#ifndef _IPC_ALLOC_H
+#define _IPC_ALLOC_H
+
+#include <linux/types.h>
+
+/* Rare, potentially huge allocations.  Both functions may sleep. */
+void *ipc_alloc(size_t size);
+void ipc_free(void *ptr, size_t size);
+
+/*
+ * RCU-managed IPC storage. Objects start with reference count 1; the putref
+ * that reaches zero schedules destruction after the RCU grace period.
+ */
+void *ipc_rcu_alloc(size_t size);
+void ipc_rcu_getref(void *ptr);
+void ipc_rcu_putref(void *ptr);
+
+#endif /* _IPC_ALLOC_H */
diff --git a/ipc/util.h b/ipc/util.h
index e92cf14794b25d9bb7ed38c4f94f7bca1a83631f..25ab073e630b4488bdd832ff77fdb681d82b0cf8 100644
--- a/ipc/util.h
+++ b/ipc/util.h
@@ -12,6 +12,9 @@
 
 #include <linux/unistd.h>
 #include <linux/err.h>
+#include <linux/ipc.h>
+
+#include "alloc.h"
 
 #define SEQ_MULTIPLIER	(IPCMNI)
 
@@ -105,22 +108,6 @@ void ipc_rmid(struct ipc_ids *, struct kern_ipc_perm *);
 /* must be called with ipcp locked */
 int ipcperms(struct ipc_namespace *ns, struct kern_ipc_perm *ipcp, short flg);
 
-/* for rare, potentially huge allocations.
- * both function can sleep
- */
-void *ipc_alloc(size_t size);
-void ipc_free(void *ptr, size_t size);
-
-/*
- * For allocation that need to be freed by RCU.
- * Objects are reference counted, they start with reference count 1.
- * getref increases the refcount, the putref call that reduces the recount
- * to 0 schedules the rcu destruction. Caller must guarantee locking.
- */
-void *ipc_rcu_alloc(size_t size);
-void ipc_rcu_getref(void *ptr);
-void ipc_rcu_putref(void *ptr);
-
 struct kern_ipc_perm *ipc_lock(struct ipc_ids *, int);
 
 void kernel_to_ipc64_perm(struct kern_ipc_perm *in, struct ipc64_perm *out);
diff --git a/windows_verify/source_contract_test.py b/windows_verify/source_contract_test.py
index d2c465e3a171fca2e8cc7b367260688532791c24..8e50cbed56c41d6ab7aac6cc40126e0e0f998374 100644
--- a/windows_verify/source_contract_test.py
+++ b/windows_verify/source_contract_test.py
@@ -50,6 +50,7 @@ def check_ipc_allocator_split() -> None:
     makefile = read("ipc/Makefile")
     util = read("ipc/util.c")
     alloc = read("ipc/alloc.c")
+    alloc_h = read("ipc/alloc.h")
     util_h = read("ipc/util.h")
 
     require("util.o alloc.o msgutil.o" in makefile,
@@ -61,13 +62,31 @@ def check_ipc_allocator_split() -> None:
     require("void *ipc_alloc(size_t size)" in alloc and
             "void *ipc_rcu_alloc(size_t size)" in alloc,
             "allocator implementations must live in ipc/alloc.c")
+    require('#include "alloc.h"' in alloc,
+            "ipc/alloc.c must include its focused interface header")
+    forbid(alloc, '#include "util.h"',
+           "ipc/alloc.c must not depend on the full IPC util.h graph")
+    require("#include <linux/types.h>" in alloc_h,
+            "ipc/alloc.h must define size_t through a direct Linux type dependency")
+    for prototype in (
+        "void *ipc_alloc(size_t size);",
+        "void ipc_free(void *ptr, size_t size);",
+        "void *ipc_rcu_alloc(size_t size);",
+        "void ipc_rcu_getref(void *ptr);",
+        "void ipc_rcu_putref(void *ptr);",
+    ):
+        require(prototype in alloc_h,
+                f"missing allocator interface prototype: {prototype}")
+    require('#include "alloc.h"' in util_h,
+            "ipc/util.h must re-export the focused allocator interface")
+    require("#include <linux/ipc.h>" in util_h,
+            "ipc/util.h must directly provide IPCMNI/kern_ipc_perm dependencies")
+    require("ipc_alloc(size_t size)" not in util_h,
+            "allocator prototypes must not be duplicated in ipc/util.h")
     require("ipc_rcu_init_header" in alloc and "ipc_rcu_header_size" in alloc,
             "RCU allocation policy must be factored into named helpers")
     require("l30_size_add_overflow(header_size, size, &allocation_size)" in alloc,
             "RCU header + object size must use checked addition")
-    require("ipc_alloc(size_t size)" in util_h and
-            "ipc_free(void *ptr, size_t size)" in util_h,
-            "IPC allocation interface must use size_t")
 
 
 def check_block_sysfs() -> None:
@@ -156,14 +175,18 @@ def check_ci_scope() -> None:
         "block/scsi_ioctl.o",
         "ipc/alloc.o",
         "ipc/mqueue.o",
+        "ipc/msg.o",
         "ipc/msgutil.o",
         "ipc/sem.o",
+        "ipc/shm.o",
         "ipc/util.o",
     )
     for obj in required_objects:
         require(obj in build, f"GCC 4.8 proof build does not compile {obj}")
     require("cp ipc/alloc.c linux-3.0/ipc/alloc.c" in workflow,
             "CI overlay must include new ipc/alloc.c")
+    require("cp ipc/alloc.h linux-3.0/ipc/alloc.h" in workflow,
+            "CI overlay must include focused ipc/alloc.h")
     require("cp ipc/Makefile linux-3.0/ipc/Makefile" in workflow,
             "CI overlay must include the refactored IPC Makefile")
     require("gcc:4.8.5" in workflow,
